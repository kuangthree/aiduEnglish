<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ecnu.ireader">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".view_controller.IMainActivity">
        </activity>
        <activity android:name=".view_controller.WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".view_controller.LibraryActivity">
        </activity>
        <activity android:name=".view_controller.DictionaryActivity">
        </activity>
        <activity android:name=".view_controller.CollectionActivity">
        </activity>
        <activity android:name=".view_controller.CustomizeReadActivity">
        </activity>
        <activity android:name=".view_controller.WordActivity">
        </activity>
        <activity android:name=".view_controller.ReadActivity">
        </activity>
    </application>

</manifest>